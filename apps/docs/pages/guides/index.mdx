import Layout from '~/layouts/DefaultLayout'
import Link from 'next/link'
import { GlassPanel, IconPanel, Button, IconChevronRight } from 'ui'

export const meta = {
  id: 'guides',
  title: 'Guides',
  description: 'Guides',
}

### Getting Started

<div className="grid lg:grid-cols-12 gap-6 not-prose">
  <Link href="/guides/getting-started" passHref>
    <a className={'col-span-4'}>
      <GlassPanel title="Getting Started" span="col-span-6" background={false}>
        Experience the ease of creating an API through a model first and declarative approach.
      </GlassPanel>
    </a>
  </Link>

  <Link href="/guides/adding-permissions" passHref>
    <a className={'col-span-4'}>
      <GlassPanel title="Adding Permissions" span="col-span-6" background={false}>
        Add Authentication and Authorization for user specific permissions.
      </GlassPanel>
    </a>
  </Link>
</div>

export const Page = ({ children }) => <Layout meta={meta} children={children} />

export default Page
